✅ STEP 1 — Create ast_symbol_graph.py

Create file:

server/ast_symbol_graph.py

Paste FULL code:

import ast
import os
from collections import defaultdict


class ASTSymbolGraph:
    def __init__(self):
        self.function_defs = defaultdict(list)
        self.function_calls = defaultdict(list)

    def scan_repo(self, repo_path: str):
        self.function_defs.clear()
        self.function_calls.clear()

        for root, _, files in os.walk(repo_path):
            for file in files:
                if file.endswith(".py"):
                    path = os.path.join(root, file)
                    self._process_python_file(path)

    def _process_python_file(self, path: str):
        try:
            with open(path, "r", encoding="utf-8") as f:
                tree = ast.parse(f.read())
        except Exception:
            return

        for node in ast.walk(tree):
            if isinstance(node, ast.FunctionDef):
                self.function_defs[node.name].append({
                    "file": path,
                    "line": node.lineno,
                })

            if isinstance(node, ast.Call):
                if isinstance(node.func, ast.Name):
                    self.function_calls[node.func.id].append({
                        "file": path,
                        "line": node.lineno,
                    })

    def get_definition(self, func_name: str):
        return self.function_defs.get(func_name, [])

    def get_call_sites(self, func_name: str):
        return self.function_calls.get(func_name, [])
🧪 STEP 2 — Test AST graph (VERY IMPORTANT)

Open Python REPL and run:

from ast_symbol_graph import ASTSymbolGraph

g = ASTSymbolGraph()
g.scan_repo(r"C:\Users\adity\Downloads\testkyc")

print(g.get_definition("main"))
print(g.get_call_sites("main"))
✅ Expected behavior

You should see something like:

[{'file': '...script.js', 'line': 1}]
[{'file': '...script.js', 'line': 5}]

If yes → you just crossed into real tooling territory.

🧠 Where you stand right now (honest assessment)
✅ You HAVE

semantic search

chunking

reranker

dependency mapper

history engine

UI with metrics

clickable navigation

⚠️ You STILL lack

true structural reasoning

cross-file call certainty

architecture awareness

AST graph fixes this.

🔥 Your next HIGH-ROI integration (after testing)

Once AST graph works, next step will be:

merge AST graph into dependency mapper

That’s when things get serious.

✅ Do this now

Fix your REPL usage

Add ast_symbol_graph.py

Run the test I gave

Tell me the output

Then we push to the next level.

To become recruiter-bait, you still need:

JS structural understanding

cross-file reasoning

impact analysis

semantic history

surgical patching